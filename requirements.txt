# ============================================================
#  연예인 픽템 쇼츠 자막 공장 — Python 패키지 목록
#  Streamlit Community Cloud 배포용
#
#  주의: torch 는 openai-whisper 의존성으로 자동 설치됨
#        별도로 torch 를 추가하면 버전 충돌 가능 — 절대 추가 금지
# ============================================================

# 핵심 UI
streamlit>=1.35.0

# 영상 처리 (ffmpeg 바이너리는 packages.txt 에서 설치)
Pillow>=10.0.0

# 자막 자동 추출 (torch 는 자동으로 같이 설치됨)
openai-whisper>=20231117

# 고급 옵션 전용 (원본 찾기, 후보 검색)
yt-dlp>=2024.1.1

# 기타 유틸
numpy>=1.24.0
requests>=2.31.0
