# Streamlit Cloud requirements -- minimal safe version
# faster-whisper removed: heavy deps (onnxruntime+ctranslate2+av) exceed 1GB free tier
# Whisper transcription is optional -- users can type subtitles manually

streamlit>=1.35.0
Pillow>=10.0.0
numpy>=1.24.0
requests>=2.31.0
yt-dlp>=2024.1.1
